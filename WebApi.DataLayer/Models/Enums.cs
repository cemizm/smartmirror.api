using System;
namespace WebApi.DataLayer.Models
{
    public enum WidgetType
    {
        Weather = 1,
    }

    public enum WidgetSide
    {
        Inactive = 0,
        Left = 1,
        Right = 2,
    }
}
